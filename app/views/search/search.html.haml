%h1= @user.name

- if @user.repositories.empty?
  .row
    .col-md-8.col-md-offset-2
      .card
        .card-block
          .card-text.text-warning The user has no repository
- else
  - @user.repositories.each do |repo|
    .row
      .col-md-8.col-md-offset-2
        .card.repository
          .card-block
            %h4.card-title= repo.name
            %p.card-text= repo.updated_at
